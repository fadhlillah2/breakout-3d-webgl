{
  "name": "breakout-3d-webgl",
  "private": true,
  "version": "1.0.0",
  "description": "Breakout 3D rendered with a hand-written WebGL2 pipeline — no engine, no runtime dependencies.",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "smoke": "node tools/smoke.mjs",
    "screenshot": "node scripts/screenshot.mjs"
  }
}
