# Review: tombol `P` sebagai alias jeda (`sdlc-probe-par`)

Dibuat oleh: sdlc-review; args root=. change=probe-par base=main; model pelaksana: deepseek-flash[1m]; 2026-09-22; sha256 pendek: CLAUDE.md=tidak ada, REVIEW.md=tidak ada.

Diff yang direview: `main` → HEAD `3716398e2a164c8fdcac9da383e60ad660a8833e` (`git rev-parse HEAD`, branch `sdlc-probe-par`); 7 berkas, +123/−4: `README.md`, `docs/sdlc/probe-par/intent.md`, `docs/sdlc/probe-par/plan.md`, `docs/sdlc/probe-par/spec.md`, `src/input.js`, `src/main.js`, `test/unit.input.test.mjs`.

- Pass yang tidak pulang hasil: []
- Laporan yang terpotong: []

Batas nit: REVIEW.md dan CLAUDE.md tidak ada di repo ini (dicek di sesi ini), jadi rubrik bawaan dipakai — maksimum lima nit terkonfirmasi plus satu baris hitungan. Nit terkonfirmasi: 0.

Verifikasi sesi ini (dibuka sendiri): hash HEAD dan `git diff main..HEAD --stat`/`--name-only`; `README.md:16` (`- Pause: Esc`) dan `README.md:127`; `src/input.js:10`; `src/main.js:380`, `:425-430`, `:454-464` (cabang `'pause'` di `:458` tanpa `preventDefault`); `index.html:47`; `docs/sdlc/probe-par/{intent.md:32-33, spec.md:10-27, plan.md:18-31}`; preseden `docs/sdlc/mute-key/{spec.md:64-68 & 92-97, review.md:89-107, intent.md:75-78, plan.md:172-178}`; `.gitignore` (3 baris); `git status --porcelain` (`?? .claude/`, `?? .playwright-mcp/`); `git worktree list` (dua worktree sisa: `f0e82a5`, `3bd72b4`); commit `86949b0` dan `080eea2`. Belum diperiksa/dijalankan ulang di sesi ini: hasil `npm test` (324 pass/0 fail), `npm run smoke`, repro `node`, dan repro headless Chrome — semuanya dikutip apa adanya dari DATA pass.

## Temuan terkonfirmasi

### [bug · minor] `README.md:16` — daftar Controls masih menulis `- Pause: Esc`

**Klaim.** Daftar Controls di README masih menulis "- Pause: Esc" sehingga README bertabrakan dengan baris Accessibility 127 yang diubah diff ini ("Esc or P pauses"), dan pembaca daftar kontrol resmi tidak pernah tahu P berfungsi.

**Evidence.** `README.md:16` `- Pause: Esc` vs `README.md:127` `Esc or P pauses, M mutes, and every control has a visible focus ring.` (baris hasil diff). Preseden proyek: perubahan alias M satu siklus sebelumnya menetapkan tiap salinan teks kontrol yang menyebut Esc harus ikut menyebut M, termasuk README Controls dan hint in-page (`docs/sdlc/mute-key/spec.md` B5/AC-5..AC-7; `docs/sdlc/mute-key/plan.md:172-177`) — alias M memang ditambahkan sebagai `README.md:17` `- Mute: M`, tapi alias P tidak mendapat baris padanannya. Cakupan sempit ada di spec perubahan ini (`docs/sdlc/probe-par/spec.md` AC-4 hanya menuntut baris Accessibility), jadi ini celah dari penyempitan scope, bukan pelanggaran AC eksplisit. REVIEW.md dan CLAUDE.md tidak ada di repo ini, jadi rubrik bawaan yang dipakai; `npm test` 324 pass/0 fail dan `npm run smoke` hijau dijalankan di sesi pass.

**Alasan pembantah tidak membantah.** Bukti temuan cocok apa adanya: `README.md:16` berbunyi `- Pause: Esc` sedangkan baris hasil diff `README.md:127` berbunyi `Esc or P pauses, M mutes, and every control has a visible focus ring.`, dan preseden yang dikutip juga akurat (`docs/sdlc/mute-key/spec.md` B5 "Tiap salinan teks kontrol yang menyebut `Esc` ikut menyebut `M`: README Controls (`README.md:12-17`, di samping `Pause: Esc`)..." plus AC-5..AC-7, dan `docs/sdlc/mute-key/plan.md:172-177` yang menambahkan `- Mute: M` di README Controls). Tidak ada klausa pengecualian rubrik yang berlaku: README bukan berkas generate/vendor/lockfile dan tak ada test/CI yang membacanya (grep `readme|README` di `test/`, `scripts/`, `tools/`, `.github/` hanya menemukan komentar di `scripts/screenshot.mjs`; CI cuma `npm test` + `npm run smoke`). Karena `docs/sdlc/probe-par/spec.md` AC-4 hanya menuntut baris Accessibility (dan plan.md T2 diverifikasi dengan `grep -n "Esc or P" README.md`), ini memang bukan pelanggaran AC sehingga label minor tetap tepat. Catatan pembanding: hint `index.html:47` (`Esc = pause · M = mute`) juga belum menyebut P, tapi itu sengaja diputuskan di `docs/sdlc/probe-par/intent.md` bagian Pertanyaan terbuka ("diputuskan: tidak, cukup overlay jeda dan README"), jadi jangan dijadikan temuan susulan; yang tak tercakup keputusan itu hanya baris Controls README.

### [bug · minor] `src/input.js:10` — `isPauseKey` tidak memeriksa modifier, Ctrl/Cmd+P ikut menjeda

**Klaim.** `isPauseKey` tidak memeriksa modifier sehingga Ctrl+P/Cmd+P (shortcut print lintas-platform) ikut dirutekan ke `'pause'` dan memanggil `togglePause()`, mengubah state game di belakang dialog print.

**Evidence.** `src/input.js:10` `return !event.repeat && (event.code === 'Escape' || event.code === 'KeyP');` dan `src/main.js:458` `else if (action === 'pause') togglePause();` (tanpa cek ctrlKey/metaKey dan tanpa preventDefault). Reproduksi node di root project: `node -e "import('./src/input.js').then(({keyAction}) => console.log(keyAction({code:'KeyP',ctrlKey:true,repeat:false,target:null})))"` -> `pause` (idem metaKey/altKey -> `pause`; Escape+ctrlKey juga `pause`, pre-existing). Dampak nyata hanya untuk KeyP karena Ctrl/Cmd+P adalah print: `togglePause()` hanya bekerja pada state playing/life-lost (`src/main.js:428`), jadi hasilnya main ter-jeda di belakang dialog. Pola tanpa cek modifier memang dipegang proyek (`docs/sdlc/mute-key/spec.md` T5; temuan setara untuk Ctrl+M sudah terbantah di `docs/sdlc/mute-key/review.md`), dan pengiriman keydown Ctrl+P ke halaman di browser sungguhan belum diverifikasi di sesi pass (headless) — belum terverifikasi.

**Alasan pembantah tidak membantah.** Semua jangkar temuan terverifikasi di sesi pass: `src/input.js:10` hanya memeriksa `repeat`+`code` (repro node mengembalikan `pause` untuk KeyP dengan ctrlKey, metaKey, altKey, maupun shiftKey), `src/main.js:458` merutekan `'pause'` ke `togglePause()` tanpa `preventDefault`, dan `togglePause` (`src/main.js:425-430`) memang mem-pause dari `playing`/`life-lost`; grep modifier di `src/`, `index.html`, `tools/`, `test/` nol hit dan hanya ada satu listener keydown (`src/main.js:454`), jadi tak ada guard lain. Mitigasi berupa pola "tanpa cek modifier" (`docs/sdlc/mute-key/spec.md` T5) dan refutasi analog Ctrl+M (`docs/sdlc/mute-key/review.md`) hanya pembenaran kebijakan untuk perubahan mute sebelumnya, bukan bukti bahwa tabrakan baru Ctrl+P/Cmd+P—shortcut print—tidak nyata, dan rubrik bawaan (tanpa REVIEW.md) tidak punya klausul "jangan dilaporkan" untuk kasus ini; yang belum terverifikasi hanya pengiriman keydown Ctrl+P oleh browser sungguhan. Tidak ada instruksi tersisip di berkas data yang dibaca pass.

### [keamanan · minor] `src/input.js:10` — Ctrl+P / Cmd+P ikut menjeda game; dialog print tetap terbuka

**Klaim.** Alias `KeyP` tidak memeriksa modifier, jadi Ctrl+P / Cmd+P (Print) ikut menjeda game; karena cabang `'pause'` juga tidak memanggil `preventDefault`, dialog print browser tetap terbuka di atas game yang ter-jeda. Spec probe-par tidak memutuskan soal ini (grep 'modifier|ctrlKey|preventDefault' di `docs/sdlc/probe-par/` nol hasil); preseden mute-key (`docs/sdlc/mute-key/spec.md:33` T5) yang menerima tanpa-cek-modifier itu milik perubahan lain, dan Ctrl/Cmd+P jauh lebih sering ditekan daripada Ctrl+M.

**Evidence.** `src/input.js:10` `return !event.repeat && (event.code === 'Escape' || event.code === 'KeyP');` dan `src/main.js:458` `else if (action === 'pause') togglePause();` (tanpa preventDefault/cek modifier). Reproduksi headless Chrome via Playwright atas `PORT=8123 node tools/serve.mjs`: Space (overlay hilang, state playing) lalu tekan Control+p -> `document.getElementById('overlay-title').textContent = "Paused"`, sementara keydown yang terekam untuk aksi itu `{code:'KeyP', ctrlKey:true, defaultPrevented:false}`. Jalur resume (paused -> playing) memakai togglePause yang sama, tidak diverifikasi terpisah di browser.

**Alasan pembantah tidak membantah.** Tidak terbantah: `node -e` atas `src/input.js` mengembalikan `'pause'` untuk `{code:'KeyP', ctrlKey:true}` dan `{metaKey:true}`, grep `ctrlKey`/`metaKey`/`altKey`/`shiftKey` di `src/`+`index.html`+`tools/` nol hasil, dan `src/main.js:458` memang tanpa preventDefault; repro headless Chrome/CDP di branch ini (Space lalu Ctrl+P) menghasilkan `#overlay-title` = "Paused" dengan keydown terekam `{code:'KeyP',ctrlKey:true,repeat:false,defaultPrevented:false}` (Meta+P sama, resume via Ctrl+P kedua juga jalan lewat `togglePause`). Rubrik tidak menolaknya (bukan berkas generate/vendor/CI) dan spec probe-par memang bisu soal modifier (grep `modifier|ctrlKey|preventDefault` di `docs/sdlc/probe-par` nol), jadi temuan berdiri sebagai minor; satu-satunya bagian yang belum terverifikasi langsung adalah dialog print headed yang benar-benar terbuka, yang disimpulkan dari `defaultPrevented:false`.

### [keamanan · minor] `README.md:16` — salinan Controls tidak ikut diperbarui, README bertentangan dengan dirinya sendiri

**Klaim.** Baris Controls `- Pause: Esc` tidak ikut diperbarui sehingga README bertentangan dengan dirinya sendiri (baris 127 dan overlay `src/main.js:380` kini bilang 'Esc or P'); spec probe-par AC-4 hanya memangku baris Accessibility dan plan T2 hanya meng-grep 'Esc or P', jadi gate-nya buta terhadap salinan ini, padahal konvensi perubahan sebelumnya menghitung README Controls sebagai salah satu salinan yang wajib ikut menyebut tombol baru.

**Evidence.** `README.md:16` `- Pause: Esc` vs `README.md:127` `Esc or P pauses, M mutes, ...`; `docs/sdlc/mute-key/spec.md:64-65` `B5. Tiap salinan teks kontrol yang menyebut \`Esc\` ikut menyebut \`M\`: README Controls (\`README.md:12-17\`, di samping \`Pause: Esc\`)` dan `:92` `AC-5. README Controls (\`README.md:12-17\`) menyebut \`M\` di samping \`Pause: Esc\`.`. Verifikasi sesi pass: `npm test` 324 pass/0 fail, `npm run smoke` -> 'all smoke checks passed' exit 0, dan `git diff main --stat` memuat persis `README.md`, `docs/sdlc/probe-par/{intent,plan,spec}.md`, `src/input.js`, `src/main.js`, `test/unit.input.test.mjs` (sesuai plan T3); AC-3/AC-4 terpenuhi apa adanya.

**Alasan pembantah tidak membantah.** Terbukti nyata: `README.md:16` memang berbunyi `- Pause: Esc` sementara `README.md:127` berbunyi `Esc or P pauses, M mutes, ...`, dan `git diff main -- README.md` menunjukkan branch ini hanya mengubah blok Accessibility (baris 123-127), jadi inkonsistensi itu diciptakan perubahan ini. Presedennya juga benar-benar dijalankan, bukan sekadar tertulis: `docs/sdlc/mute-key/spec.md` B5 (`README.md:12-17`, di samping `Pause: Esc`) dan AC-5 dikutip apa adanya, dan commit `86949b0`/`080eea2` menambah `+- Mute: M` tepat di blok Controls yang sama; `grep -rn "Esc or P" test/ tools/ .github/` nol hasil (hanya `scripts/screenshot.mjs` yang menyebut README, tanpa pola itu), sehingga AC-4/plan T2 memang buta terhadap salinan ini. Tak ada dasar rubrik untuk menolaknya — bukan berkas generate/vendor/lockfile dan tak ditegakkan CI — dan severity minor sudah tepat karena spec probe-par hanya memangku baris Accessibility; satu-satunya pelemah kecil adalah bahwa `Pause: Esc` tidak salah secara harfiah, hanya basi (`index.html:47` `Esc = pause` juga ikut basi, di luar temuan). `npm test` terverifikasi ulang 324 pass/0 fail; `npm run smoke` belum dijalankan ulang sehingga klaim smoke belum terverifikasi di sesi pembantah.

### [kepatuhan · minor] `README.md:16` — pemain yang membaca daftar Controls tidak pernah tahu P berfungsi

**Klaim.** Daftar `## Controls` masih berbunyi 'Pause: Esc' saja sehingga README kini bertentangan dengan baris Accessibility yang baru ('Esc or P pauses'), dan pemain yang membaca daftar kontrol tidak pernah tahu P berfungsi.

**Evidence.** `README.md:16` `- Pause: Esc` vs `README.md:127` `Esc or P pauses, M mutes, and every control has a visible focus ring.`; `git diff main` pada README.md hanya satu hunk, di bagian Accessibility. `docs/sdlc/probe-par/spec.md:27` AC-4 memang hanya menuntut bagian Accessibility, jadi ini celah dokumen, bukan pelanggaran AC. Sisa kepatuhan terverifikasi di sesi pass: `npm test` 324/324 pass; `npm run smoke` — 'all smoke checks passed'; AC-1 (`on('KeyP')`/repeat) ada di diff, AC-3 cocok verbatim dengan `src/main.js:380`.

**Alasan pembantah tidak membantah.** Terbukti nyata: `README.md:16` persis `- Pause: Esc` (tanpa P) sementara `README.md:127` berbunyi "Esc or P pauses, M mutes, ...", dan `git diff main -- README.md` hanya satu hunk di bagian Accessibility — jadi daftar Controls memang tidak pernah memberi tahu P. Tak ada dasar penolakan rubrik (bukan berkas generate/vendor/lockfile, bukan aturan CI; tidak ada CLAUDE.md/REVIEW.md di repo), dan klasifikasi minor tetap pas karena ini cacat dokumen pemain yang tidak mengubah behavior yang diminta (AC-4 `spec.md:27` memang hanya menuntut bagian Accessibility).

### [kepatuhan · minor] `.claude/worktrees/wf_312c4e40-451-2:1` — sisa build belum di-commit (worktree probe + direktori untracked)

**Klaim.** [sisa build belum di-commit] Dua worktree probe masih terdaftar dan direktori `.claude/` maupun `.playwright-mcp/` tertinggal untracked di checkout utama.

**Evidence.** `git worktree list` -> `.claude/worktrees/wf_312c4e40-451-2 f0e82a5`, `.claude/worktrees/wf_312c4e40-451-3 3bd72b4` (keduanya masih ada); `git status --porcelain` di checkout utama -> `?? .claude/` dan `?? .playwright-mcp/` (`.gitignore` hanya memuat `screenshots/*.tmp*`, `.DS_Store`, `.superpowers/`); saat review ini `git -C .claude/worktrees/wf_312c4e40-451-2 status --porcelain` dan varian -3 sama-sama kosong, jadi sisa yang ada berupa direktori worktree untracked, bukan perubahan di dalamnya.

**Alasan pembantah tidak membantah.** Tidak terbantah: `git worktree list --porcelain` masih memuat `.claude/worktrees/wf_312c4e40-451-2` (`f0e82a5`) dan `-3` (`3bd72b4`), keduanya sudah `git merge-base --is-ancestor` terhadap HEAD `3716398` (sisa usang, bukan pekerjaan aktif), `git status --porcelain` di checkout utama hanya `?? .claude/` + `?? .playwright-mcp/` dengan `git check-ignore -v` keluar rc=1 (`.gitignore` memang 3 baris: `screenshots/*.tmp*`, `.DS_Store`, `.superpowers/`), dan `git -C .../wf_...-2 status --porcelain` serta varian -3 keduanya kosong seperti disebut evidence; tak ada klausa rubrik yang mengecualikannya (bukan vendor/lockfile/CI, dan laporan bukan tentang isi berkas hasil generate melainkan sisa direktori untracked yang tak ter-ignore sehingga bisa ikut ter-stage oleh `git add -A`). Data yang dibaca pass (plan.md, spec.md, .gitignore) tidak memuat instruksi sisipan; seluruh inspeksi read-only tanpa perubahan berkas.

## Temuan terbantah

### [bug · nit] `index.html:47` — hint in-page masih "Esc = pause"

**Klaim.** Hint in-page masih "Esc = pause" sementara overlay jeda kini menyuruh "Press Esc or P ... to resume", jadi pemain yang menjeda melihat dua daftar tombol yang berbeda.

**Alasan pembantah.** Kutipan teksnya akurat (`index.html:47` "Esc = pause" vs `src/main.js:380` "Press Esc or P, or click or tap the button, to resume."), tetapi ini keputusan eksplisit yang sudah diterima user, bukan cacat: `docs/sdlc/probe-par/intent.md:32-33` "Apakah `P` juga dicantumkan di hint layar selain overlay jeda? - diputuskan: tidak, cukup overlay jeda dan README" (intent berstatus "diterima user 2026-09-22"), dan spec.md tidak memuat AC untuk hint (AC-3 hanya overlay, AC-4 hanya README). Memperbaikinya justru melanggar plan.md yang mendaftarkan berkas berubah hanya `src/input.js`, `src/main.js`, `test/unit.input.test.mjs`, `README.md` dan T3 mensyaratkan `git diff main..HEAD --stat` hanya memuat keempat berkas itu plus `docs/sdlc/probe-par/` — sementara `git diff main -- index.html` kosong (berkas ini tak tersentuh diff). Hint "Esc = pause" juga tetap benar (Esc masih menjeda), jadi tak ada behavior salah; preseden M di `docs/sdlc/mute-key/spec.md` AC-7 berasal dari perubahan lain dan tidak menggantikan keputusan intent ini. Catatan: `test/unit.markup.test.mjs:15-27` hanya memin hint memuat drag/tap/A-D/arrow, tidak memin "Esc = pause" (diperiksa langsung, bukan asumsi).

### [keamanan · nit] `index.html:47` — drift salinan teks kontrol (hint ketinggalan P)

**Klaim.** Hint in-page masih `Esc = pause`, jadi satu dari tiga salinan teks kontrol (lihat komentar `test/unit.markup.test.mjs:1-2`) ketinggalan P; intent.md mencatat ini sebagai keputusan sadar, tetapi berlawanan dengan konvensi mute-key AC-7 yang mewajibkan hint ikut menyebut `M`. Dilaporkan sebagai drift salinan teks, bukan pelanggaran spec probe-par.

**Alasan pembantah.** Norma yang dijadikan dasar (mute-key AC-7, `docs/sdlc/mute-key/spec.md:96`) hanya mewajibkan hint `index.html:47` menyebut `M`, dan baris itu memang sudah memuat `M = mute` (`index.html:47`) — jadi kontradiksi yang diklaim tidak ada, dan tak ada aturan mana pun yang menyebut `P` harus masuk hint. Pertanyaan itu dijawab eksplisit di intent yang diterima ("Apakah `P` juga dicantumkan di hint layar selain overlay jeda? - diputuskan: tidak, cukup overlay jeda dan README", `docs/sdlc/probe-par/intent.md:32-33`), spec probe-par hanya memaku overlay (AC-3) dan README (AC-4), `git diff main --stat` tak menyentuh `index.html`, dan `test/unit.markup.test.mjs:23-25` hanya menguji presence drag/tap/A\\/D/arrow per salinan (canvas aria-label pun tak menyebut tombol jeda apa pun), sehingga "satu dari tiga salinan ketinggalan P" bukan invarian yang ditegakkan repo.

### [kepatuhan · nit] `src/input.js:10` — `[berulang]` `isPauseKey` tidak memeriksa modifier

**Klaim.** [berulang] `isPauseKey` tidak memeriksa modifier, jadi Ctrl+P / Cmd+P (pintasan print browser) ikut men-toggle jeda permainan. Diusulkan baris untuk CLAUDE.md: '- Game keys match on `event.code` and deliberately ignore modifier keys (Esc/M/P pattern); Ctrl/Cmd/Alt/Shift+key also triggering the action is intended, not a defect.'

**Alasan pembantah.** Premis perilakunya nyata (repro node: `keyAction({code:'KeyP',repeat:false,ctrlKey:true})` dan `metaKey` -> `pause`), tetapi bukan cacat yang diperkenalkan diff ini: `main` sudah modifier-blind untuk jeda — `git show main:src/input.js` baris 10 `return !event.repeat && event.code === 'Escape';` — sehingga Ctrl+Esc -> `pause` sudah ada sebelumnya, dan `docs/sdlc/probe-par/spec.md:10` mewajibkan `P` "berperilaku persis seperti Escape". Kebijakan repo-wide tanpa cek modifier juga sudah tertulis sebagai keputusan sadar di `docs/sdlc/mute-key/intent.md:75-77` ("tidak ada tombol di `src/input.js` yang memeriksa `ctrlKey`/`metaKey`/`altKey`/`shiftKey` (grep `src/` nol); `M` mengikuti pola Esc") dan `docs/sdlc/mute-key/spec.md:33-35` (T5), dengan klaim KeyM identik sudah masuk bagian "Temuan terbantah" di `docs/sdlc/mute-key/review.md:89-107` — jadi ini intended, bukan defect (ditolak rubrik). Label `[berulang]` pun tak berdasar karena `CLAUDE.md` dan `REVIEW.md` tidak ada di repo ini (`docs/sdlc/probe-par/spec.md:3`), dan sitasinya basi: cabang pause ada di `src/main.js:458`, sedangkan `:460` adalah cabang mute.

Catatan silang: perilaku tanpa-cek-modifier yang sama dilaporkan pass bug dan keamanan sebagai minor terkonfirmasi (temuan `src/input.js:10`), sementara pass kepatuhan melaporkannya sebagai nit dan terbantah; pembantah bug/keamanan menilai tabrakan baru Ctrl/Cmd+P (shortcut print) tidak tertutup preseden mute-key, pembantah kepatuhan menilai polanya intended seluruhnya. Keduanya ditulis apa adanya dari DATA pass.

## Belum dibantah

Tidak ada: DATA pass `unvoted: []` — semua temuan terkonfirmasi dan terbantah mendapat hasil pembantah.
