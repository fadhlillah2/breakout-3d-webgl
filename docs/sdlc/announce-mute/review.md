# Review announce-mute: `935fc69` → HEAD

- Diff yang direview: `935fc69` → HEAD; HEAD = `b9f5eff3545bbca20078f70314bbb87bde2c2f88` (`git rev-parse HEAD`). Berkas dalam rentang: README.md, docs/sdlc/announce-mute/plan.md, src/main.js, test/unit.input.test.mjs.
- Pass yang tidak pulang hasil: `[]`
- Laporan yang terpotong: `[]`

Catatan penulis: hash HEAD, rentang diff, dan daftar berkas diperiksa langsung dengan git; teks klaim/evidence tiap temuan dikutip dari data adjudikasi pass, tidak dijalankan ulang oleh penulis berkas ini.

## Temuan terkonfirmasi

### 1. minor — `src/main.js:462` (pass: bug)

**Klaim:** Guard `document.activeElement !== muteButton` membuat 'M' saat fokus di #mute sepenuhnya tanpa pengumuman live region, dan satu-satunya umpan balik yang tersisa bersandar pada klaim AS-2 yang spec sendiri sebut belum diverifikasi dengan screen reader; bila klaim itu keliru, pemain screen reader yang fokus di tombol tetap tanpa umpan balik - persis kelas pengguna yang jadi sasaran perubahan ini. Bukan pelanggaran spec (T5/B2 memang meminta guard ini).

**Evidence:** src/main.js:462 `if (document.activeElement !== muteButton) announce.textContent = sfx.muted ? 'Sound muted.' : 'Sound on.';`. spec.md:104-106 AS-2: "Klaim bahwa screen reader mengumumkan sendiri perubahan `aria-pressed` tombol `#mute` yang fokus belum diverifikasi dengan screen reader; ia dasar B2/B3", dan spec.md:117 OQ-2 menaruh perluasannya di luar scope. README.md:129 mengklaim region mengumumkan "a keyboard mute toggle" tanpa menyebut pengecualian fokus-tombol (plan.md risiko 5 mengakui selisih ini). Verifikasi screen reader tidak tersedia di lingkungan ini - belum terverifikasi, bukan terbukti gagal.

**Alasan pembantah tidak membantah (confidence: high):** Semua premis temuan terverifikasi dan skenarionya nyata: src/main.js:462 (baris di dalam diff) memang melewatkan penulisan `#announce` saat `document.activeElement === muteButton`, dan src/input.js merutekan `KeyM` ke aksi 'mute' walau fokus di tombol (dipaku test/unit.input.test.mjs:31-32, "a focused button never gates M"), sehingga reproduksi DOM nyata (Playwright, `data-gl=ok`, event tuts asli) menunjukkan tekan M saat fokus di `#mute` mengubah `aria-pressed`→'false' dan label→'Mute' sementara satu-satunya live region (index.html:48; grep aria-live hanya satu) tidak ditulis dan tertinggal basi "Sound muted.". Dependensi pada AS-2 juga persis seperti dikutip: spec.md:104-106 menyebut klaim itu belum diverifikasi dengan screen reader, OQ-2 (:117-119) menaruh perluasannya di luar scope, README.md:129 memang mengklaim "keyboard mute toggle" tanpa pengecualian fokus-tombol, dan plan risiko 4-5 mengakui keduanya; temuan juga benar bahwa T5/B2 (spec.md:36-39, :56-57) meminta guard ini, jadi severity minor konsisten dan tidak ada klausa rubrik (default; REVIEW.md tidak ada di repo) yang menolaknya. Satu-satunya ketidaktepatan halus — frasa "satu-satunya umpan balik bersandar pada AS-2" padahal `syncMute` (src/main.js:61-62) juga menukar label Mute/Unmute — tidak membatalkan temuan karena kanal label itu pun bersandar pada perilaku screen reader yang belum diverifikasi (kelas yang sama dengan AS-2).

### 2. minor — `docs/sdlc/announce-mute/plan.md:159` (pass: bug)

**Klaim:** Gate Task 4 butir 3 (diulang di butir 3 "Bukti selesai keseluruhan", :198) menuntut `git status --short` menampilkan `?? docs/sdlc/announce-mute/plan.md`, padahal plan.md sudah tracked sejak commit basis 935fc69 sebelum Task 1 dijalankan, jadi gate itu tidak bisa lolos apa adanya.

**Evidence:** `git show --stat 935fc69` -> `docs/sdlc/announce-mute/plan.md | 209 +++`; `git status --short` pada tree kini kosong (exit 0). Catatan lindung: kalimat itu sudah ada di tree basis dan di luar hunk diff ini (diff plan.md hanya mengisi blok "Bukti merah", :207-233, yang saya reproduksi di salinan scratchpad dan cocok persis: `not ok 85`, 108 tests / 107 pass / 1 fail, lokasi `unit.input.test.mjs:45:1`, output `npm test` 1169 baris, dan `node --test test/unit.input.test.mjs` 3 pass/1 fail `ERR_ASSERTION`).

**Alasan pembantah tidak membantah (confidence: high):** Terverifikasi: :159 memang menuntut `git status --short` menampilkan `?? docs/sdlc/announce-mute/plan.md` (blame 935fc69) dan :198 mengulang `plan.md` untracked terpisah, sementara 935fc69 sendiri yang menambahkan plan.md (`git show --stat 935fc69`: `plan.md | 209 +++`, 1 file changed) dan `git ls-tree 540e7e4` membuktikannya sudah tracked saat Task 1; reproduksi `git status --short` (juga untuk plan.md saja) kosong exit 0, dan berkas tracked tak pernah bisa tampil `??` — gate mustahil lolos apa adanya. Barisnya memang di luar hunk diff plan.md (hanya @@ -205,5 +205,28 @@, blok "Bukti merah", seperti dicatat finding) tapi plan.md adalah berkas yang tersentuh diff dan REVIEW.md tidak ada (test -f exit 1) sehingga rubrik bawaan berlaku tanpa pengecualian, jadi temuan tidak salah, bisa terjadi pada keadaan nyata tree ini, dan tak ditolak rubrik.

### 3. minor — `docs/sdlc/announce-mute/plan.md:212` (pass: keamanan)

**Klaim:** Blok bukti merah yang di-commit memuat path absolut mesin lokal beserta username (path absolut mesin lokal berikut username) — satu-satunya kemunculan pola itu di repo, dan repo punya remote publik, sehingga data identitas mesin kontributor ikut terekspos bila branch di-merge/push.

**Evidence:** Baris (ditambahkan di 540e7e4, bagian rentang yang direview): `location: 'test/unit.input.test.mjs:45:1'`. `grep -rn <username> --exclude-dir=.git .` dan `grep -rn '/home/' --exclude-dir=.git . (path absolut)` sama-sama hanya mencetak baris ini. `git remote -v` → `origin https://github.com/fadhlillah2/breakout-3d-webgl.git`; package.json homepage `https://fadhlillah2.github.io/breakout-3d-webgl/`. Branch `sdlc-announce` tanpa upstream (`git status -sb` → `## sdlc-announce`), jadi belum ter-push, tapi isi berkas ini masuk ke main lewat merge. Bukan credential, hanya username lokal.

**Alasan pembantah tidak membantah (confidence: high):** Tidak bisa dibantah: baris 212 docs/sdlc/announce-mute/plan.md persis berisi `location: 'test/unit.input.test.mjs:45:1'`, ditambahkan di 540e7e4 yang ada di rentang 935fc69..HEAD (terlihat sebagai baris `+` di `git diff 935fc69`, dan `git log -S` atas path itu menunjuk 540e7e4), berkas tracked, dan grep username maupun `/home/` --exclude-dir=.git hanya mencetak baris itu. `whoami` mengonfirmasi itu username mesin lokal; rubrik bawaan tidak mengecualikan docs/plan.md, dan publik-tidaknya remote GitHub tidak diverifikasi (belum diperiksa, tanpa jaringan) tapi hanya pemberat, bukan inti klaim.

### 4. minor — `test/unit.input.test.mjs:47` (pass: keamanan)

**Klaim:** Penghapus komentar pada pin hanya membuang baris yang seluruhnya `//`, jadi pin tetap hijau saat isi cabang mute dikomentari dengan `/* */` (atau saat urutan yang dituntut ditaruh di komentar `//` ekor setelah `{`) — pernyataan "a commented-out statement must not satisfy the pin" tidak tercapai untuk bentuk komentar itu.

**Evidence:** Cuplikan: baris 46-47 `readFileSync(...).replace(/^\s*\/\/.*$/gm, '')` dengan komentar `// a commented-out statement must not satisfy the pin`. Probe (skrip di scratchpad, membaca regex persis dari berkas ini) atas varian in-memory src/main.js: `current tree, pin green: true`; `block-commented branch satisfies pin: true` (regex cocok lewat `[^}]*` yang melintasi `/*`); `trailing-comment branch satisfies pin: true`; `full-line-commented branch satisfies pin: false`. Artinya regresi mengganti cabang dengan blok terkomentari tetap lolos CI. Tidak mengubah behavior aplikasi; melemahkan kekuatan pin terhadap regresi.

**Alasan pembantah tidak membantah (confidence: high):** Terverifikasi nyata: `test/unit.input.test.mjs:47` berisi `.replace(/^\s*\/\/.*$/gm, '')` (baris baru di diff ini, commit `540e7e4`) yang hanya membuang baris penuh `//`. Menjalankan berkas test asli terhadap salinan mirror `src/main.js`: isi cabang mute di-`/* */` → pin HIJAU (suite penuh 108 pass/0 fail exit 0; `npm run smoke` exit 0 "all smoke checks passed") walau `sfx.toggle()`/`syncMute()`/`announce` tak pernah jalan; urutan tuntutan di komentar `//` ekor setelah `{` dengan penulisan `announce` absen dari kode → pin HIJAU (108/0 exit 0); sedangkan bentuk komentar baris penuh `//` → pin MERAH (3/1). Tak ada gate penambal: `unit.sfx.test.mjs:108-114` hanya daftar nama `sfx.play`, `unit.markup.test.mjs:59` hanya literal `overlayText`, `tools/smoke.mjs:106` hanya announce game-over. Severitas minor cocok rubrik (behavior aplikasi tak berubah; pin tetap memenuhi transisi merah→hijau AC-1) dan bukan kelas yang dilarang dilaporkan. Catatan presisi: memblok-komentari hanya baris `announce` (bukan seluruh isi cabang) tetap merah, karena `/*` yang menyela antara `syncMute();` dan `if` memutus kedekatan literal yang dituntut regex — mempersempit bentuk false-pass, tidak membatalkan temuan.

### 5. nit — `docs/sdlc/announce-mute/plan.md:119` (pass: kepatuhan)

**Klaim:** Klaim plan di :119 (diulang di :177) bahwa blok `{ ... }` membuat pin Task 1 tetap merah tidak benar—pin tetap cocok (hijau) saat statement announce dibungkus blok multi-baris, dan klaim itu bertentangan dengan catatan Task 1 di :66-67 yang menyebut kurung kurawal opsional serta layout multi-baris sama-sama cocok; teks ini pra-diff (di luar hunk 205+), bukan diperkenalkan perubahan ini.

**Evidence:** Probe node atas src/main.js kini: ganti `if (document.activeElement !== muteButton) announce.textContent = ...;` dengan bentuk blok multi-baris `if (...) {\n announce.textContent = sfx.muted ? 'Sound muted.' : 'Sound on.';\n }` lalu uji regex pin plan.md:58 -> matches: true (hijau); hanya bentuk ternary terpenggal (`sfx.muted\n ? 'Sound muted.'\n : 'Sound on.';`) yang menghasilkan matches: false (merah). Kutipan plan: :66-67 'kurung kurawal guard opsional (`\{?`) dan layout satu atau multi-baris sama-sama cocok' vs :118-119 'Guard sengaja brace-free mengikuti pola `:406-407`; blok `{ ... }` atau ternary terpenggal membuat pin Task 1 tetap merah.'

**Alasan pembantah tidak membantah (confidence: high):** Tidak terrefutasi: probe node dengan regex pin plan.md:58 — byte-identik (`true`) dengan pin di test/unit.input.test.mjs — menghasilkan matches=true untuk bentuk blok guard multi-baris `if (document.activeElement !== muteButton) { announce.textContent = sfx.muted ? 'Sound muted.' : 'Sound on.'; }`, termasuk saat baris itu disubstitusi ke salinan utuh src/main.js, dan matches=false hanya untuk ternary terpenggal; jadi klaim di :118-119 (klausa "...blok `{ ... }` ... tetap merah" selesai di :119) dan :177 memang salah dan berlawanan dengan :66-67 ("kurung kurawal guard opsional (`\{?`) dan layout satu atau multi-baris sama-sama cocok"). Diff 935fc69 atas plan.md hanya menyentuh hunk @@ -205,5 +205,28 @@ sehingga klaim itu memang teks pra-diff, dan `node --test test/unit.input.test.mjs` kini 4 pass/0 fail.

### 6. nit — `docs/sdlc/announce-mute/plan.md:157` (pass: kepatuhan)

**Klaim:** Checklist verifikasi plan (:157-159 dan :198) mengharapkan `git diff --name-only` menghasilkan persis tiga berkas implementasi dengan plan.md untracked, tetapi plan.md ikut ter-commit di 540e7e4 sehingga pada rentang review 935fc69..HEAD name-only memuat empat berkas; substansi lingkup implementasi tetap tiga berkas dan plan.md memang tercantum sebagai artefak di tabel plan (:38).

**Evidence:** `git diff 935fc69 --name-only` -> README.md, docs/sdlc/announce-mute/plan.md, src/main.js, test/unit.input.test.mjs; `git diff 935fc69 --stat` mencatat `docs/sdlc/announce-mute/plan.md | 25 ++++++++`; `git show --stat 540e7e4` -> plan.md + test/unit.input.test.mjs, `git log --oneline -1 540e7e4 -- docs/sdlc/announce-mute/plan.md` mengonfirmasi tracking sejak commit itu; working tree bersih sehingga `git diff --name-only` tanpa basis (perintah literal Task 4) kini kosong, bukan tiga berkas.

**Alasan pembantah tidak membantah (confidence: high):** Tidak bisa dibantah: plan.md :157-159 menuntut `git diff --name-only` -> persis README.md/src/main.js/test/unit.input.test.mjs dengan `?? docs/sdlc/announce-mute/plan.md` di `git status --short`, dan :198 "tiga berkas implementasi saja; plan.md untracked terpisah", sedangkan `git diff 935fc69 --name-only` menghasilkan empat berkas (README.md, docs/sdlc/announce-mute/plan.md, src/main.js, test/unit.input.test.mjs) dan `git show --stat 540e7e4` mencatat plan.md +25 baris (blok "Bukti merah") bersama test/unit.input.test.mjs; working tree bersih sehingga perintah literal tanpa basis kini kosong, dan baris :38 memang mencantumkan plan.md sebagai artefak. Satu impresi minor: tracking plan.md sebenarnya dimulai di commit basis 935fc69 ("Add the SDLC plan", 209 baris), bukan di 540e7e4 yang hanya meng-commit perubahannya — nuansa ini bahkan memperkuat ketidaksesuaian checklist, bukan membatalkannya; severity nit sesuai rubrik karena substansi lingkup tetap tiga berkas.

## Temuan terbantah

### 1. nit — `src/main.js:462` (pass: bug)

**Klaim:** Karena #announce merangkap baris status yang terlihat (style.css:68 `.status`), mute dari keyboard kini menimpa pesan status yang sedang tampil (mis. "Life lost. 2 remaining.", "Game over. Score N.", atau pesan context lost) dan teks itu bertahan sampai penulisan status berikutnya; plan.md:106-113 menerima konsekuensi ini secara eksplisit sebagai konsekuensi yang diketahui.

**Alasan pembantah (confidence: medium):** Semua kutipan terverifikasi apa adanya (main.js:462 menulis announce.textContent; index.html:48 dan style.css:68 benar; smoke.mjs:106 tak menekan M; nol dispatchEvent/KeyboardEvent dan nol timer), dan pemanggilnya nyata: keyAction mengembalikan 'mute' untuk KeyM di segala state. Tetapi ini bukan cacat yang ditolak rubrik: penulisan itu diwajibkan spec (B1 mewajibkan #announce berisi 'Sound muted.'/'Sound on.' sesudah M; T2/T3 mengunci region yang ada tanpa elemen baru) dan plan.md:106-113 menyatakan efek visual ini 'diterima sebagai konsekuensi yang diketahui' — jadi perilaku sesuai kontrak; temuan hanya mengulang konsekuensi yang sudah diputuskan plan, tanpa perilaku salah, kehilangan data, atau usul perbaikan yang masih patuh spec.

## Belum dibantah

Tidak ada; `unvoted` kosong (`[]`).
